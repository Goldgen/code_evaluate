#include<stdio.h>
int main()
{
    //?????????
    unsigned x,p,n,y1,y2,y;
    scanf("%u %u %u",&x,&p,&n);

    if(n==0){
        y1 = (~x&((1<<(p-n+2))-1))&((~0)<<(p));
        y2 = ((~0)^((1<<(p-n+2))-1)|((1<<(p))-1))&x;
    }
    else{
		y1 = (~x&((1<<(p+1))-1))&((~0)<<(p-n+1));//?????????0
        y2 = ((~0)^((1<<(p+1))-1)|((1<<(p-n+1))-1))&x;//????0?????

    }
    y = y1 | y2;

    printf("%u",y);

    return 0;
}