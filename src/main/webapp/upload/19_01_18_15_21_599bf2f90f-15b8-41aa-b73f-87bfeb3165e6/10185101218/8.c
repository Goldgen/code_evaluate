#include<stdio.h>
int main(void)
{
	int a,b,c,d;
	scanf("%d%d%d",&a,&b,&c);
	if(a>=b&&a>=c)      //??a??????
	{
		if(b>=c)
			d=b;       //b?c???????
		else
			d=c;
	}
	else if(a>=b||a>=c)//a??????????????????
		d=a;
	else if(b>=c)//a??????b?c????????
		d=c;
	else
		d=b;
	printf("%d",d);
	return 0;
}