#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <math.h>
#include <ctype.h>
int main()
{
    int a;
    int i=-1,j=0,k=1;

    scanf("%d",&a);
    if(a>0)
        printf("%d",k);
    else if(a==0)
        printf("%d",j);
    else
        printf("%d",i);

    return 0;
}