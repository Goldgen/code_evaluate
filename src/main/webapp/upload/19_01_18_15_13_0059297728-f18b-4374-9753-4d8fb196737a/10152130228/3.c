#include <stdio.h>
#include <stdlib.h>

/*?????????????x, p, n????????x??????p???p-n+1??????x??????????
????????

??
100 6 2
4*/

unsigned invert(unsigned long x, int p, int n)
{
    return x ^ (~(~0 << n) << (p + 1 - n));
}

int main()
{
    unsigned long x, y;
    int p, n;
    scanf("%lu %d %d", &x, &p, &n);
    y = invert(x, p, n);
    printf("%lu", y);
    return 0;
}

