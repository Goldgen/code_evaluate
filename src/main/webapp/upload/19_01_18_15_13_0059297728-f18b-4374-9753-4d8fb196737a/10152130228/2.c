#include <stdio.h>
#include <stdlib.h>

/*?????????????x, p, n????????x??????p???p-n+1?(????)???
?????????????0??
?????????????

??
100 6 2
3*/

unsigned getbits(unsigned long x, int p, int n)
{
    return   (x>>(p-n+1)) & ~(~0<<n) ;
}


int main()
{
    unsigned long x, y;
    int p, n;
    scanf("%lu %d %d", &x, &p, &n);
    y = getbits(x, p, n);
    printf("%lu", y);
    return 0;
}
