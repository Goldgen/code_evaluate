#include <stdio.h>
#include <limits.h>

int main()
{
    char ch1,ch2;
    int in1,in2;
    ch1=CHAR_MIN;
    ch2=CHAR_MAX;
    in1=INT_MAX;
    in2=INT_MIN;
	printf("%d\n%d\n%d\n%d",ch2,ch1,in1,in2);

}